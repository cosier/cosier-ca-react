import {
    Component
} from 'react';

import {
    navigate
} from 'actions/NavigationActions';

export default class Footer extends Component {
    render() {
        return (
            <div className='footer'/>
        );
    }
}
